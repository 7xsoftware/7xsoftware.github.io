---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="7xSoftware - Desarrollo de aplicaciones Android vanguardistas y software de alto rendimiento." />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" href="/favicon2.png" />
    <meta property="og:title" content="7xSoftware | Innovación en Software" />
    <meta property="og:description" content="Desarrollo de aplicaciones Android y soluciones de software interactivas y fluidas." />
    <meta property="og:image" content="/wattmx_preview.png" />
    <meta property="og:url" content="https://7xsoftware.github.io" />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="7xSoftware" />
    <meta name="google-site-verification" content="vkb7FH1eKGym-KKtJaIvuVyfyHO4Ncc2gTHMwHWpDtE" />

    <title>{title}</title>
  </head>
  <body class="bg-[--background] sm:px-28 lg:px-20 px-9">
    <slot />
  </body>
</html>

<style is:global>
  @import url('https://fonts.cdnfonts.com/css/montserrat');

  :root {
    --background: #101010;
    --sec: #a476ff;
    --white: #dfdfdf;
    --white-icon: #f3f3f398;
    --white-icon-tr: #f3f3f310;
  }

  html {
    scroll-behavior: smooth;
  }

  * {
    font-family:
      "Montserrat",
      -apple-system,
      system-ui,
      sans-serif;
    box-sizing: border-box;
    padding: 0;
    margin: 0;
  }

  *::selection {
    background-color: var(--sec);
    color: var(--background);
  }

  /* Scrollbar styles */

  ::-webkit-scrollbar {
    width: 10px;
  }

  ::-webkit-scrollbar-track {
    background: var(--background);
  }

  ::-webkit-scrollbar-thumb {
    background: #333;
    border-radius: 10px;
    border: 2px solid var(--background);
  }

  ::-webkit-scrollbar-thumb:hover {
    background: var(--sec);
  }

  /* Scrollbar styles for Firefox */
  * {
    scrollbar-width: thin;
    scrollbar-color: #333 var(--background);
  }
</style>
